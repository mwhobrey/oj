version: 1.0.{build}-{branch}

environment:
  matrix:
    - RUBY_VERSION: 23-x64

build: off

install:
  - set PATH=C:\Ruby%RUBY_VERSION%\bin;%PATH%
  - gem install oj

build_script:
  - cd C:\projects\oj\ext\oj
  - ruby extconf.rb
  - make

before_test:
  - ruby -v
  - gem -v
  - bundle -v
